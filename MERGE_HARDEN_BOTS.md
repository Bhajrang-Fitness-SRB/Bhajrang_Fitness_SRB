## Merge harden-bots into default branch

This is a merge commit placeholder that records the merge of the `harden-bots` branch into the repository default branch `Bhajrang-Fitness-SRB`.

Commits merged:
- chore: harden bots - logging, env checks, timeouts, safer backups
- feat: add AI orchestrator, analytics, attendance, payment reminders and supporting stubs

Note: This file is intentionally small and is only used to create a merge commit when applied via the API.
